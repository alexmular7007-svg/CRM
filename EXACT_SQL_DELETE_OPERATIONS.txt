╔════════════════════════════════════════════════════════════════════════════════════╗
║           EXACT SQL OPERATIONS EXECUTED DURING WORKSPACE DELETION                  ║
║                  (Proof of deletion for each entity)                               ║
╚════════════════════════════════════════════════════════════════════════════════════╝

SCENARIO: User deletes Workspace with ID = 123

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 1: ATTACHMENTS (EXECUTED FIRST)
═══════════════════════════════════════════════════════════════════════════════════════

STEP 1.1: Delete all attachments in workspace
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM attachments WHERE workspace_id = 123

EXPLANATION:
  - Directly uses workspace_id column (added in V14 migration)
  - Deletes ALL attachments in workspace regardless of parent (chat_message or task)
  - CRITICAL: Done BEFORE chat_messages and tasks to prevent FK violations

ROWS AFFECTED: 
  Example: If workspace has 5 chat attachments + 3 task attachments
  Result: 8 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)
FLUSH:  ✅ entityManager.flush() ensures deletion persisted to DB

────────────────────────────────────────────────────────────────────────────────────

STEP 1.2: Delete task attachment metadata
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM task_attachments WHERE task_id IN (
    SELECT id FROM tasks WHERE workspace_id = 123
  )

EXPLANATION:
  - Separate entity tracking task→attachment relationships
  - Foreign keys: task_id → tasks.id
  - Already safe because tasks still exist at this point

ROWS AFFECTED:
  Example: If workspace has 10 task attachments
  Result: 10 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #1:
  entityManager.flush()
  Ensures PostgreSQL knows attachments are gone before deleting parents

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 2: TASK-RELATED DATA
═══════════════════════════════════════════════════════════════════════════════════════

STEP 2.1: Delete mentions (references task_comments)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM mentions WHERE comment_id IN (
    SELECT id FROM task_comments WHERE task_id IN (
      SELECT id FROM tasks WHERE workspace_id = 123
    )
  )

EXPLANATION:
  - Mentions have FK to task_comments
  - Must delete BEFORE deleting task_comments to respect FK constraint
  - Foreign key: mentions.comment_id → task_comments.id

ROWS AFFECTED:
  Example: If workspace has 20 mentions
  Result: 20 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 2.2: Delete task comments (references tasks)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM task_comments WHERE task_id IN (
    SELECT id FROM tasks WHERE workspace_id = 123
  )

EXPLANATION:
  - Task comments have FK to tasks
  - Can only delete AFTER deleting mentions (which reference comments)
  - Foreign key: task_comments.task_id → tasks.id

ROWS AFFECTED:
  Example: If workspace has 15 task comments
  Result: 15 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 2.3: Delete task activities
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM task_activities WHERE task_id IN (
    SELECT id FROM tasks WHERE workspace_id = 123
  )

EXPLANATION:
  - Task activities record task history/events
  - Foreign key: task_activities.task_id → tasks.id
  - Can delete before parent task

ROWS AFFECTED:
  Example: If workspace has 50 task activity records
  Result: 50 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 2.4: Delete task watchers
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM task_watchers WHERE task_id IN (
    SELECT id FROM tasks WHERE workspace_id = 123
  )

EXPLANATION:
  - Task watchers track which users are watching tasks
  - Foreign key: task_watchers.task_id → tasks.id
  - Can delete before parent task

ROWS AFFECTED:
  Example: If workspace has 30 task watchers
  Result: 30 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 2.5: Delete tasks (parent entity)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM tasks WHERE workspace_id = 123

EXPLANATION:
  - Delete tasks AFTER all task-related children
  - Direct workspace_id comparison (fast and safe)
  - Foreign key: tasks.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 25 tasks
  Result: 25 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #2:
  entityManager.flush()
  Ensures all task data is deleted from database

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 3: LEAD-RELATED DATA
═══════════════════════════════════════════════════════════════════════════════════════

STEP 3.1: Delete lead activities
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM lead_activities WHERE lead_id IN (
    SELECT id FROM leads WHERE workspace_id = 123
  )

EXPLANATION:
  - Lead activities record lead history/changes
  - Foreign key: lead_activities.lead_id → leads.id
  - Must delete BEFORE deleting leads

ROWS AFFECTED:
  Example: If workspace has 40 lead activity records
  Result: 40 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 3.2: Delete leads (parent entity)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM leads WHERE workspace_id = 123

EXPLANATION:
  - Delete leads AFTER all lead-related children
  - Direct workspace_id comparison
  - Foreign key: leads.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 12 leads
  Result: 12 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #3:
  entityManager.flush()
  Ensures all lead data is deleted from database

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 4: CHAT-RELATED DATA (SAFE - All attachments already deleted)
═══════════════════════════════════════════════════════════════════════════════════════

STEP 4.1: Delete chat participants
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM chat_participants WHERE chat_room_id IN (
    SELECT id FROM chat_rooms WHERE workspace_id = 123
  )

EXPLANATION:
  - Chat participants track who is in each chat room
  - Foreign key: chat_participants.chat_room_id → chat_rooms.id
  - Must delete BEFORE chat_rooms

ROWS AFFECTED:
  Example: If workspace has 100 chat participants across rooms
  Result: 100 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 4.2: Delete chat messages (CRITICAL POINT)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM chat_messages WHERE chat_room_id IN (
    SELECT id FROM chat_rooms WHERE workspace_id = 123
  )

EXPLANATION:
  - Chat messages have FK to attachments
  - Foreign key: chat_messages.attachment_id → attachments.id
  - THIS IS WHERE THE ORIGINAL BUG OCCURRED
  - NOW SAFE because all attachments already deleted in Phase 1
  - All attachment records are gone before this query executes

ROWS AFFECTED:
  Example: If workspace has 500 chat messages
  Result: 500 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)
FIX APPLIED: Phase 1 executes before this, so no orphaned attachments

────────────────────────────────────────────────────────────────────────────────────

STEP 4.3: Delete chat rooms (parent entity)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM chat_rooms WHERE workspace_id = 123

EXPLANATION:
  - Delete chat rooms AFTER all chat participants and messages
  - Direct workspace_id comparison
  - Foreign key: chat_rooms.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 8 chat rooms
  Result: 8 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #4:
  entityManager.flush()
  Ensures all chat data is deleted from database

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 5: PROJECT-RELATED DATA
═══════════════════════════════════════════════════════════════════════════════════════

STEP 5.1: Delete project members
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM project_members WHERE project_id IN (
    SELECT id FROM projects WHERE workspace_id = 123
  )

EXPLANATION:
  - Project members track which users are in projects
  - Foreign key: project_members.project_id → projects.id
  - Must delete BEFORE deleting projects

ROWS AFFECTED:
  Example: If workspace has 45 project members
  Result: 45 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 5.2: Delete projects (parent entity)
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM projects WHERE workspace_id = 123

EXPLANATION:
  - Delete projects AFTER all project members
  - Direct workspace_id comparison
  - Foreign key: projects.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 6 projects
  Result: 6 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #5:
  entityManager.flush()
  Ensures all project data is deleted from database

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 6: WORKSPACE METADATA & ANALYTICS
═══════════════════════════════════════════════════════════════════════════════════════

STEP 6.1: Delete AI insight snapshots
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM ai_insight_snapshots WHERE workspace_id = 123

EXPLANATION:
  - AI analytics data for workspace
  - Direct workspace_id comparison
  - Foreign key: ai_insight_snapshots.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 100 insight records
  Result: 100 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 6.2: Delete notifications
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM notifications WHERE workspace_id = 123

EXPLANATION:
  - Notification records for workspace
  - Direct workspace_id comparison
  - Foreign key: notifications.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 200 notification records
  Result: 200 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 6.3: Delete workspace invitations
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM workspace_invitations WHERE workspace_id = 123

EXPLANATION:
  - Pending invitations to join workspace
  - Direct workspace_id comparison
  - Foreign key: workspace_invitations.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 3 pending invitations
  Result: 3 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

STEP 6.4: Delete workspace members
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM workspace_members WHERE workspace_id = 123

EXPLANATION:
  - Users who are members of workspace
  - Direct workspace_id comparison
  - Foreign key: workspace_members.workspace_id → workspaces.id

ROWS AFFECTED:
  Example: If workspace has 5 members (owner + 4 invited)
  Result: 5 rows deleted

STATUS: ✅ @Modifying(clearAutomatically = true)

────────────────────────────────────────────────────────────────────────────────────

FLUSH POINT #6:
  entityManager.flush()
  Ensures all metadata is deleted from database

═══════════════════════════════════════════════════════════════════════════════════════
PHASE 7: ROOT WORKSPACE ENTITY
═══════════════════════════════════════════════════════════════════════════════════════

STEP 7: Delete workspace
──────────────────────────────────────────────────────────────────────────────────────
SQL GENERATED:
  DELETE FROM workspaces WHERE id = 123

EXPLANATION:
  - At this point, NO child entities exist
  - All FKs already deleted or resolved
  - Safe to delete root workspace
  - Foreign key: (none - workspace is root, only has owner_id → users.id which is not deleted)

ROWS AFFECTED:
  Result: 1 row deleted (the workspace itself)

STATUS: ✓ Repository.delete(workspace)

═══════════════════════════════════════════════════════════════════════════════════════
SUMMARY - COMPLETE DELETION FLOW
═══════════════════════════════════════════════════════════════════════════════════════

Total SQL Operations: 20 DELETE statements

Execution Order:
  1. ✓ Attachments (8 rows) - FIRST because of FKs from chat_messages and tasks
  2. ✓ Mentions (20 rows) - Before task_comments
  3. ✓ Task Comments (15 rows) - Before tasks
  4. ✓ Task Activities (50 rows)
  5. ✓ Task Watchers (30 rows)
  6. ✓ Tasks (25 rows)
  7. ✓ Lead Activities (40 rows)
  8. ✓ Leads (12 rows)
  9. ✓ Chat Participants (100 rows)
 10. ✓ Chat Messages (500 rows) - NOW SAFE (attachments already deleted)
 11. ✓ Chat Rooms (8 rows)
 12. ✓ Project Members (45 rows)
 13. ✓ Projects (6 rows)
 14. ✓ AI Insights (100 rows)
 15. ✓ Notifications (200 rows)
 16. ✓ Invitations (3 rows)
 17. ✓ Members (5 rows)
 18. ✓ Workspace (1 row)
─────────────────────────
Total Rows Deleted: 1,168 rows

Flush Points: 6 (after each phase to ensure DB consistency)

Status: ✅ NO FK VIOLATIONS - All dependencies respected

═══════════════════════════════════════════════════════════════════════════════════════
VERIFICATION - EXPECTED RESULTS AFTER DELETION
═══════════════════════════════════════════════════════════════════════════════════════

SELECT COUNT(*) FROM attachments WHERE workspace_id = 123;
Result: 0 ✓

SELECT COUNT(*) FROM chat_messages WHERE chat_room_id IN (
  SELECT id FROM chat_rooms WHERE workspace_id = 123
);
Result: 0 ✓

SELECT COUNT(*) FROM tasks WHERE workspace_id = 123;
Result: 0 ✓

SELECT COUNT(*) FROM workspaces WHERE id = 123;
Result: 0 ✓

═══════════════════════════════════════════════════════════════════════════════════════
