================================================================================
                          TESTING CHECKLIST
              Complete Invitation Workflow Verification
================================================================================

START HERE: Run these tests to verify all 12 requirements work

================================================================================
                          SETUP (Do Once)
================================================================================

1. Start Backend:
   mvn spring-boot:run
   
2. Start Frontend:
   npm run dev
   
3. Create Test Accounts in the app:
   - Account 1: owner@test.com / password123
   - Account 2: invited@test.com / password123
   - Account 3: (for Google/GitHub testing - use existing Google/GitHub accounts)

================================================================================
                    TEST 1: EMAIL/PASSWORD FLOW
================================================================================

SCENARIO: User receives invitation via email and accepts via email/password login

SETUP:
  1. Login as owner@test.com
  2. Create workspace: "Test Workspace Email"
  3. Invite invited@test.com as MEMBER
  4. Find invitation email in inbox (or check email logs)

TEST STEPS:
  1. In DIFFERENT browser/incognito window
  2. Open email link from invitation
  3. Verify: Redirect to /login page (not already accepted)
  4. Enter credentials: invited@test.com / password123
  5. Click Login
  6. Verify: Auto-redirects to workspace dashboard (NO manual accept click)
  7. Verify: Workspace "Test Workspace Email" appears in sidebar
  8. Verify: Can navigate to workspace content

VERIFY DATABASE:
  Run these SQL queries:
  
  SELECT status, accepted_by, accepted_at FROM workspace_invitations 
  WHERE email = 'invited@test.com' AND status = 'ACCEPTED'
  ✓ Should return: status=ACCEPTED, accepted_by={user_id}, accepted_at={timestamp}
  
  SELECT role, status FROM workspace_members 
  WHERE workspace_id = (SELECT id FROM workspaces WHERE name = 'Test Workspace Email')
  AND user_id = (SELECT id FROM users WHERE email = 'invited@test.com')
  ✓ Should return: role=MEMBER, status=ACTIVE

VERIFY NOTIFICATIONS:
  Check notification panel for both users:
  
  Owner should see:
    Title: "Invitation Accepted"
    Message: "invited@test.com accepted your invitation as MEMBER"
  
  Invited user should see:
    Title: "Joined Workspace"
    Message: "You joined workspace: Test Workspace Email"

STATUS:
  [ ] Email sent and received
  [ ] Can click invitation link
  [ ] Redirect to login works
  [ ] Auto-accept after login (no manual click)
  [ ] Workspace appears in sidebar
  [ ] Database shows ACCEPTED status
  [ ] Member created with role=MEMBER
  [ ] Both parties receive notifications

================================================================================
                    TEST 2: GOOGLE OAUTH FLOW
================================================================================

SCENARIO: User receives invitation and accepts via Google OAuth

SETUP:
  1. Login as owner (using Google OAuth with account: owner-google@gmail.com)
  2. Create workspace: "Test Workspace Google"
  3. Invite: jane-google@gmail.com as ADMIN
  4. Find invitation email

TEST STEPS:
  1. In DIFFERENT browser/incognito
  2. Open email link
  3. See login page with "Login with Google"
  4. Click "Login with Google"
  5. Authenticate with jane-google@gmail.com
  6. Verify: Auto-redirects to workspace (NO manual accept)
  7. Verify: Workspace appears in sidebar immediately
  8. Verify: Role is ADMIN (can see admin features)

VERIFY DATABASE:
  SELECT role, status FROM workspace_members 
  WHERE workspace_id = (SELECT id FROM workspaces WHERE name = 'Test Workspace Google')
  AND user_id = (SELECT id FROM users WHERE email = 'jane-google@gmail.com')
  ✓ Should return: role=ADMIN, status=ACTIVE

VERIFY NOTIFICATIONS:
  Owner should see: "jane-google@gmail.com accepted your invitation as ADMIN"
  Jane should see: "You joined workspace: Test Workspace Google"

STATUS:
  [ ] Email link opens
  [ ] Google OAuth option available
  [ ] Auto-accept after Google login
  [ ] Workspace appears with correct role
  [ ] Database shows ADMIN role
  [ ] Both parties notified

================================================================================
                    TEST 3: GITHUB OAUTH FLOW
================================================================================

SCENARIO: User receives invitation and accepts via GitHub OAuth

SETUP:
  1. Login as owner (using GitHub with account: owner-github)
  2. Create workspace: "Test Workspace GitHub"
  3. Invite: user-github@github.email as MEMBER

TEST STEPS:
  1. In DIFFERENT browser/incognito
  2. Open email link
  3. Click "Login with GitHub"
  4. Authenticate with GitHub account
  5. Verify: Auto-redirects to workspace
  6. Verify: Workspace appears immediately

VERIFY DATABASE:
  SELECT role, status FROM workspace_members 
  WHERE workspace_id = (SELECT id FROM workspaces WHERE name = 'Test Workspace GitHub')
  AND user_id = (SELECT id FROM users WHERE email LIKE '%github%')
  ✓ Should return: role=MEMBER, status=ACTIVE

STATUS:
  [ ] GitHub OAuth login available
  [ ] Auto-accept after GitHub login
  [ ] Workspace appears immediately
  [ ] Correct role assigned
  [ ] Both parties notified

================================================================================
                    TEST 4: IMMEDIATE VISIBILITY
================================================================================

SCENARIO: Workspace appears immediately without page refresh

SETUP:
  1. Login as owner@test.com
  2. Create workspace: "Immediate Test"
  3. Invite: invited@test.com as MEMBER

TEST STEPS:
  1. In DIFFERENT browser as invited@test.com
  2. Navigate to email link
  3. Login with email/password
  4. Watch sidebar closely
  5. Verify: Within 1 second, workspace appears in sidebar
  6. Verify: NO page refresh button clicked
  7. Verify: Can immediately navigate to workspace

TIMING CHECK:
  [ ] Workspace appears within 1 second
  [ ] No page refresh needed
  [ ] No manual cache clear needed

================================================================================
                    TEST 5: PRODUCTIVITY SCORE
================================================================================

SCENARIO: Productivity score updates when user accepts invitation

SETUP:
  1. Login as owner@test.com
  2. Note down owner's current productivity score (Dashboard tab)
  3. Create workspace: "Score Test"
  4. Invite: invited@test.com

TEST STEPS:
  1. As invited@test.com, accept invitation
  2. Check invited user's productivity score
  3. Create some tasks (3-5 tasks)
  4. Complete some tasks (1-2 tasks)
  5. Check score: Should be at least 3 (1 completed task = 3 points)
  6. Post comments (2-3 comments)
  7. Check score: Should increase by 2 per comment
  8. Send messages in chat
  9. Check score: Should increase by 1 per message

CALCULATION:
  Score = (tasks_completed × 3) + (comments × 2) + (messages × 1)
  
  Example:
  - 1 completed task = 3 points
  - 2 comments = 4 points
  - 3 messages = 3 points
  - Total = 10 points

STATUS:
  [ ] Productivity score updates for new workspace member
  [ ] Score increases with task completion
  [ ] Score increases with comments
  [ ] Score increases with messages
  [ ] Score calculation is correct

================================================================================
                    TEST 6: PERMISSIONS CHECK
================================================================================

SCENARIO: User has correct permissions after joining workspace

SETUP:
  Accept invitation as invited@test.com in Test Workspace Email

TEST STEPS:
  1. Sidebar shows workspace
  2. Can click to navigate
  3. Can see dashboard
  4. Can see projects tab
  5. Can see tasks tab
  6. Can see members tab
  7. Can see chat tab
  8. Can see AI tab
  9. Can see analytics/reports tab

ADMIN-SPECIFIC (if invited as ADMIN):
  1. Can see workspace settings
  2. Can invite more members
  3. Can change member roles
  4. Can delete workspace (if owner)

MEMBER-SPECIFIC (if invited as MEMBER):
  1. CANNOT see workspace settings
  2. CANNOT invite new members
  3. CANNOT change member roles
  4. Can create/edit own tasks and projects
  5. Can view shared content

STATUS:
  [ ] Can access all workspace content
  [ ] Role-based permissions working
  [ ] Cannot access restricted features
  [ ] Cannot access settings (if MEMBER)

================================================================================
                    TEST 7: ERROR SCENARIOS
================================================================================

TEST INVALID TOKEN:
  1. Manually modify invitation token in email link
  2. Try to accept with invalid token
  3. Verify: Error message appears
  4. Verify: Workspace NOT created in sidebar
  5. Verify: Status NOT updated to ACCEPTED

TEST EXPIRED INVITATION:
  1. Wait for invitation to expire (if configured)
  2. Try to accept
  3. Verify: Error message about expiration
  4. Verify: Cannot join workspace

TEST ALREADY MEMBER:
  1. As invited@test.com, try to accept same invitation twice
  2. Verify: Error that already a member
  3. Verify: Cannot duplicate membership

STATUS:
  [ ] Invalid token rejected
  [ ] Expired token rejected
  [ ] Already member prevented
  [ ] Proper error messages shown

================================================================================
                        FINAL VERIFICATION
================================================================================

OVERALL STATUS:

Email/Password Flow:
  [ ] PASS - All steps completed successfully

Google OAuth Flow:
  [ ] PASS - All steps completed successfully

GitHub OAuth Flow:
  [ ] PASS - All steps completed successfully

Immediate Visibility:
  [ ] PASS - Workspace appears without refresh

Productivity Score:
  [ ] PASS - Score calculation working

Permissions:
  [ ] PASS - Role-based permissions working

Error Scenarios:
  [ ] PASS - Errors handled correctly

================================================================================

COMPLETION CHECKLIST:

Before marking as complete, verify:

✓ All 3 auth methods work (email/password, Google, GitHub)
✓ Workspace appears immediately (no refresh)
✓ Both parties receive notifications
✓ Database shows correct role and ACCEPTED status
✓ Productivity score recalculates
✓ Permissions are correct by role
✓ Error scenarios handled properly
✓ No exceptions in logs
✓ No manual intervention needed

IF ALL ABOVE ARE ✓:
  → Implementation is PRODUCTION READY
  → No bugs detected
  → Ready for deployment

================================================================================
