import { useMemo, useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useSelector } from 'react-redux'
import { Filter, Plus, Search } from 'lucide-react'
import toast from 'react-hot-toast'
import { emailCampaignService } from '../services/emailCampaignService'
import { useWorkspaceRole } from '../hooks/useWorkspaceRole'
import EmailCampaignModal from '../components/emailcampaign/EmailCampaignModal'
import EmailCampaignTable from '../components/emailcampaign/EmailCampaignTable'

const PAGE_SIZE = 20
export default function EmailCampaigns() {
  const { currentWorkspace } = useSelector((state) => state.workspace)
  const { isAdminOrOwner } = useWorkspaceRole(currentWorkspace?.id)
  const queryClient = useQueryClient()
  const [page, setPage] = useState(0), [search, setSearch] = useState(''), [status, setStatus] = useState(''), [modal, setModal] = useState(null)
  const { data: response, isLoading, isError, refetch } = useQuery({ queryKey: ['email-campaigns', currentWorkspace?.id, page, status], queryFn: () => emailCampaignService.listCampaigns(currentWorkspace.id, { page, size: PAGE_SIZE, status: status || undefined }), enabled: !!currentWorkspace?.id })
  const remove = useMutation({ mutationFn: (id) => emailCampaignService.deleteCampaign(currentWorkspace.id, id), onSuccess: () => { toast.success('Campaign deleted successfully'); queryClient.invalidateQueries({ queryKey: ['email-campaigns'] }) }, onError: (error) => toast.error(error?.message || 'Unable to delete campaign') })
  const send = useMutation({ mutationFn: (id) => emailCampaignService.sendCampaign(currentWorkspace.id, id), onSuccess: () => { toast.success('Campaign send started'); queryClient.invalidateQueries({ queryKey: ['email-campaigns'] }) }, onError: (error) => toast.error(error?.message || 'Unable to send campaign') })
  const campaigns = useMemo(() => (response?.content || []).filter((campaign) => `${campaign.name} ${campaign.subject}`.toLowerCase().includes(search.toLowerCase())), [response, search])
  const statusOptions = ['DRAFT', 'SCHEDULED', 'SENDING', 'COMPLETED', 'PAUSED', 'CANCELLED']
  return <div className="space-y-6 p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h1 className="text-3xl font-bold text-gray-900 dark:text-white">Email Campaigns</h1><p className="mt-1 text-sm text-gray-600 dark:text-gray-400">Create, schedule, and track email campaigns.</p></div>{isAdminOrOwner && <button type="button" onClick={() => setModal({})} className="flex items-center justify-center gap-2 rounded-lg bg-violet-600 px-4 py-2 text-white hover:bg-violet-700"><Plus size={18} />New Campaign</button>}</div><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input aria-label="Search campaigns" value={search} onChange={(event) => { setSearch(event.target.value); setPage(0) }} placeholder="Search campaigns..." className="w-full rounded-lg border border-gray-200 bg-white py-2 pl-10 pr-4 text-gray-900 dark:border-[#30363D] dark:bg-[#0D1117] dark:text-white" /></div><div className="flex items-center gap-2"><Filter size={16} className="text-gray-500" /><select aria-label="Filter by campaign status" value={status} onChange={(event) => { setStatus(event.target.value); setPage(0) }} className="rounded-lg border border-gray-200 bg-white px-3 py-2 text-sm dark:border-[#30363D] dark:bg-[#0D1117] dark:text-white"><option value="">All statuses</option>{statusOptions.map((option) => <option key={option} value={option}>{option}</option>)}</select></div></div>{isLoading ? <div className="animate-pulse space-y-3 rounded-lg border border-gray-200 p-5 dark:border-[#30363D]"><div className="h-5 w-1/3 rounded bg-gray-200 dark:bg-[#21262D]" /><div className="h-12 rounded bg-gray-100 dark:bg-[#161B22]" /><div className="h-12 rounded bg-gray-100 dark:bg-[#161B22]" /></div> : isError ? <div className="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-800 dark:border-red-800 dark:bg-red-900/20 dark:text-red-200">Failed to load campaigns. <button type="button" onClick={() => refetch()} className="font-medium underline">Try again</button></div> : campaigns.length === 0 ? <div className="rounded-lg border border-dashed border-gray-300 py-12 text-center dark:border-[#30363D]"><p className="font-medium text-gray-900 dark:text-white">No email campaigns found</p><p className="mt-1 text-sm text-gray-600 dark:text-gray-400">{isAdminOrOwner ? 'Create your first campaign to get started.' : 'Campaigns created by your team will appear here.'}</p></div> : <><div className="rounded-lg border border-gray-200 bg-white dark:border-[#30363D] dark:bg-[#161B22]"><EmailCampaignTable campaigns={campaigns} canManage={isAdminOrOwner} onEdit={setModal} onDelete={(campaign) => window.confirm(`Delete campaign "${campaign.name}"?`) && remove.mutate(campaign.id)} onSend={(campaign) => window.confirm(`Send "${campaign.name}" now?`) && send.mutate(campaign.id)} /></div>{response.totalPages > 1 && <div className="flex items-center justify-end gap-3"><button type="button" disabled={page === 0} onClick={() => setPage(page - 1)} className="rounded border px-3 py-1 text-sm disabled:opacity-50">Previous</button><span className="text-sm text-gray-600 dark:text-gray-400">Page {page + 1} of {response.totalPages}</span><button type="button" disabled={page + 1 >= response.totalPages} onClick={() => setPage(page + 1)} className="rounded border px-3 py-1 text-sm disabled:opacity-50">Next</button></div>}</>}{modal !== null && <EmailCampaignModal isOpen onClose={() => setModal(null)} campaign={modal.id ? modal : null} />}</div>
}
